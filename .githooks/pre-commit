#!/bin/sh
if ! command -v markdownlint >/dev/null 2>&1; then
  echo "Error: markdownlint not found. Install with: brew install markdownlint-cli"
  exit 1
fi
markdownlint --config .markdownlint.json "**/*.md"
